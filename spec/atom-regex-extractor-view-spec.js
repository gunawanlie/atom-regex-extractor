'use babel';

import AtomRegexExtractorView from '../lib/atom-regex-extractor-view';

describe('AtomRegexExtractorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
